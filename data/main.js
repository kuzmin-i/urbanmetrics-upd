import { cover } from '../data/cover'
import { analyzes } from '../data/analyzes'
import { formatexport } from '../data/formatexport'
import { services } from '../data/popularservices'
import { price } from '../data/price'
import { formBlock } from '../data/form'


export const mainData = {
    'cover': cover,
    'analyzes': analyzes,
    'formatexport': formatexport,
    'services': services,
    'price': price,
    'formBlock': formBlock
}